apiVersion: apps/v1
kind: Deployment
metadata:
  name: vendor-dashboard
  namespace: daltaners
  labels:
    app: vendor-dashboard
    app.kubernetes.io/part-of: daltaners
    app.kubernetes.io/component: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: vendor-dashboard
  template:
    metadata:
      labels:
        app: vendor-dashboard
        app.kubernetes.io/part-of: daltaners
    spec:
      serviceAccountName: daltaners-service-account
      containers:
        - name: vendor-dashboard
          image: daltaners/vendor-dashboard:latest
          ports:
            - containerPort: 80
              protocol: TCP
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi
          startupProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 3
            failureThreshold: 10
          readinessProbe:
            httpGet:
              path: /
              port: 80
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /
              port: 80
            periodSeconds: 30
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: vendor-dashboard
  namespace: daltaners
  labels:
    app: vendor-dashboard
spec:
  selector:
    app: vendor-dashboard
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
