apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat-service
  namespace: daltaners
  labels:
    app: chat-service
    app.kubernetes.io/part-of: daltaners
    app.kubernetes.io/component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: chat-service
  template:
    metadata:
      labels:
        app: chat-service
        app.kubernetes.io/part-of: daltaners
    spec:
      serviceAccountName: daltaners-service-account
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app: chat-service
      containers:
        - name: chat-service
          image: daltaners/chat-service:latest
          ports:
            - containerPort: 3013
              protocol: TCP
          envFrom:
            - configMapRef:
                name: daltaners-config
            - secretRef:
                name: daltaners-secrets
          env:
            - name: PORT
              value: "3013"
            - name: DB_SCHEMA
              value: "chat"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          startupProbe:
            httpGet:
              path: /health
              port: 3013
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 3013
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 3013
            periodSeconds: 30
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: chat-service
  namespace: daltaners
  labels:
    app: chat-service
spec:
  selector:
    app: chat-service
  ports:
    - port: 3013
      targetPort: 3013
      protocol: TCP
  type: ClusterIP
